version: 2.1

jobs:
  hello:
    machine: true
    resource_class: exo/hardware
    steps:
      - run:
          name: Hello World
          command: echo "Hello, World!"

workflows:
  hello-workflow:
    jobs:
      - hello
