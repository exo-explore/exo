import torch
from dataclasses import dataclass

MAX_FINITE_FLOAT8E5: float
MAX_FINITE_FLOAT8E4NV: float
MAX_FINITE_FLOAT8E4B8: float

@dataclass(frozen=True)
class BaseFlexData:
    dtype: torch.dtype | None = ...
    def view(self, x: torch.Tensor): ...
    def reinterpret(self, x): ...

@dataclass(frozen=True)
class InFlexData(BaseFlexData):
    scale: torch.Tensor | None = ...
    @property
    def is_per_batch(self): ...

@dataclass(frozen=True)
class OutFlexData(BaseFlexData):
    expected_scale: torch.Tensor | None = ...
    actual_scale: torch.Tensor | None = ...
    checksum_scale: torch.Tensor | None = ...
    @property
    def is_per_batch(self): ...
    def __iter__(self): ...
